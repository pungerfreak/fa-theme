{%- if settings.date_format == 'standard' -%}{{ date_object | date: '%B %d, %Y' }}{%- else -%}{%- assign date_in_seconds = date_object | date: "%s" -%}{%- include 'timeago', date_in_seconds: date_in_seconds -%}{%- endif -%}
